# -*- coding: utf-8 -*-
# vim: ft=yaml
docker:
  manage_repo: True
  manage_awscli: False

  edition: ce

  # restart docker service on configuration updates
  service_watch: True

  buildx:
    version: v0.4.1
    install: False

  compose:
    version: 1.26.2
    pip_install: False

  slim:
    version: 1.29.0
